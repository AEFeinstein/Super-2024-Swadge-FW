#ifndef _FONT_PROCESSOR_H_
#define _FONT_PROCESSOR_H_

#include <stdbool.h>
#include "assets_preprocessor.h"

/**
 * @brief The font processor reads specially formatted font PNG images and processes
 * them into a compact bitmapped font format
 *
 */
extern const assetProcessor_t fontProcessor;

#endif